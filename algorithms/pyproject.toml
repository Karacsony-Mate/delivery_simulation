[project]
name = "algorithms"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pytest>=8.4.2",
    "raylib-py>=5.5.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
]
